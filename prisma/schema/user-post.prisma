model UserPost{
    id Int @id @default(autoincrement())
    name String
    theme String
    tags String?
    text String
    links String?
    image String?
    views Int
    likes Int
    author User @relation(fields: [authorId], references: [id])
    authorId Int
}